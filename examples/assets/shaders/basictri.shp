#include "test_include.slib"

struct vs_input
{
	float4 position : POSITION;
};

struct instance_input
{
	float4 colour : COLOR0;
};

struct vs_output
{
	float4 position : SV_POSITION0;
};

struct ps_output
{
	float4 colour : SV_Target;
};

vs_output main( vs_input _input )
{
	vs_output _output;
	
	_output.position = _input.position;
	
	return _output;
}

ps_output main( vs_output _input )
{
	ps_output _output;
	
	_output.colour = float4( 1.0f, 1.0f, 0.0f, 1.0f );
	
	_output.colour += func();
	
	{
		//test scope for the shader parser to
		//correctly find the body of the main function
	}
			
	return _output;
}